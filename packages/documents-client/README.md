# Documents client

This package is generated from `api-contracts/documents/openapi.json` with Hey API.
Use one client instance per server request so bearer tokens and correlation IDs never cross tenant boundaries.
