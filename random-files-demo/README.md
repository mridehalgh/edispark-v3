# Random Files Demo

This directory contains a small set of recreated sample files for demo and testing use.

- `alpha.txt` contains short placeholder notes
- `beta.txt` contains a few random sentences
- `sample.json` contains fake structured data
- `settings.yaml` contains fake configuration values

All content is synthetic and safe to replace or remove.
