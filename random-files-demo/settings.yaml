app:
  name: random-files-demo
  mode: sandbox
  refreshIntervalSeconds: 45

features:
  sampleLogging: true
  mockExports: false
  previewTheme: slate

storage:
  provider: local
  bucket: demo-placeholder-bucket
  retentionDays: 14
